import type { FastifyInstance } from 'fastify';

/**
 * AI / AUTOMATION / WHATSAPP feature routes — owned by Agent 3.
 * Register automation route modules here. `server.ts` never changes.
 */
export async function automationFeature(_app: FastifyInstance): Promise<void> {
  // No automation routes yet. Example:
  //   await _app.register(automationRoutes);
}
